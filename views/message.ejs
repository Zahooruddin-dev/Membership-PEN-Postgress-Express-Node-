<!DOCTYPE html>
<html>
<head>
  <title>New Message</title>
  <style>
    body {
      background: #020617;
      font-family: Arial, sans-serif;
      color: #e5e7eb;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    form {
      background: #0f172a;
      padding: 30px;
      border-radius: 8px;
      width: 500px;
    }
    input, textarea, button {
      width: 100%;
      padding: 10px;
      margin-top: 10px;
      border-radius: 4px;
      border: none;
    }
    textarea {
      height: 120px;
      resize: none;
    }
    button {
      background: #38bdf8;
      color: #020617;
      font-weight: bold;
      cursor: pointer;
    }
    .error {
      color: #f87171;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <form method="POST" action="/api/create/message">
    <h2>Create Message</h2>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <input type="text" name="title" placeholder="Title" required />
    <textarea name="message" placeholder="Message" required></textarea>
    <button type="submit">Post</button>
  </form>
</body>
</html>
